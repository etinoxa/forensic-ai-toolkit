[pytest]
addopts = -q --cov=src/fait --cov-report=term-missing
testpaths = tests
markers =
    slow: slow / downloads
    gpu: needs CUDA
filterwarnings =
    ignore:Protobuf gencode version.*:UserWarning:google.protobuf.runtime_version
    ignore:oneDNN custom operations are on:UserWarning